baremodule colors
# FC is fill color and BC is border color

# Brush outlines and fill
const brush_bc = (1.0, 0.5, 0.0, 0.5)
const brush_fc = (1.0, 0.5, 0.0, 0.2)

# The selection tool selection rectangle
const selection_selection_fc = (0.0, 1.0, 0.0, 0.6)
const selection_selection_bc = (0.0, 1.0, 0.0, 0.4)

# Selected items with selection tool
const selection_selected_fc = (1.0, 0.0, 1.0, 0.6)
const selection_selected_bc = (1.0, 0.0, 1.0, 0.4)

# Selection preview with selection tool
const selection_preview_fc = (1.0, 1.0, 0.0, 0.6)
const selection_preview_bc = (1.0, 1.0, 0.0, 0.4)

# Trigger rendering colors
const trigger_fc = (0.6, 0.2, 0.2, 0.6)
const trigger_bc = (0.6, 0.2, 0.2, 0.4)

# Entity/decal placement movement preview
const ghost_preplacement_alpha = 0.5

# Alpha of unselected rooms
const adjacent_room_alpha = 0.5

# Default background fills
const background_room_fill = (0.2, 0.2, 0.2, 1.0)
const background_canvas_fill = (0.15, 0.15, 0.15, 1.0)

# Color coded background fills
const background_room_color_coded_fill = [
    (0.20, 0.20, 0.20, 1.0),
    (0.24, 0.20, 0.20, 1.0),
    (0.20, 0.24, 0.20, 1.0),
    (0.20, 0.21, 0.22, 1.0),
    (0.20, 0.20, 0.25, 1.0),
    (0.24, 0.20, 0.25, 1.0),
    (0.22, 0.20, 0.20, 1.0)
]

# Filler room color
const filler_room_fill = (0.1, 0.4, 0.3, 1.0)

# Unknown tile color
const unknown_tile_color = (1.0, 0.0, 1.0, 1.0)

# Canvas Font color
const canvas_font_color = (0.0, 0.0, 0.0, 1.0)

end